<div class="container pb-5">
  <div class="row justify-content-center">
    <div class="col-xs-12">
      <%= image_tag "icon-student.svg" %>
      <div class="mt-3">
        <ul>
          <li><p class="info__student">Nome: <%= @student.child_name %></p></li>
          <li><p class="info__student">Endereço: <%= @student.child_address %></p></li>
          <li><p class="info__student">Responsável: <%= @student.parents_name %></p></li>
          <li><p class="info__student">Email: <%= @student.parents_email %></p></li>
      <%= link_to 'Back', :back, class: 'btn btn-primary mt-2' %>
      <%= link_to 'Edit', '#', class: 'btn btn-warning mt-2' %>
        </ul>
      </div>
    </div>
  </div>
</div>
